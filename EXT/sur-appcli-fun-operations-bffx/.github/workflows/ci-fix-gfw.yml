name: Integration fix

on:
  push:
    branches:
      - release-v[0-9]+.[0-9]+.[xX]

jobs:
  call-reusable-workflow:
    name: Integration fix
    uses: santander-group-shared-assets/gln-workflows/.github/workflows/npm-ci-tbd.yml@v1
    with:
      technology: 'npm'
    secrets: inherit
